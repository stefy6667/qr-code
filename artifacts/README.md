# QR Product Mockup Artifacts

Generated mockup ZIP bundles are intentionally **not committed** to git (to avoid binary-file workflow issues).

You can generate/export them locally using:

- `python scripts/export_mockup_archive.py`

Then share the produced ZIP from `artifacts/` via release assets, cloud storage, or direct transfer.
