services:
  - type: web
    name: qr-studio
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt && chmod +x server.py
    startCommand: python3 server.py
    disk:
      name: qr-studio-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: BASE_URL
        sync: false
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: DATA_ROOT
        value: /var/data
